import { makeStyles } from '@material-ui/core/styles';

export const useCardStyles = makeStyles({
  root: {
    maxWidth: 345,
  },
  media: {
    height: 140,
  },

  title: {
    height: '64px',
  },
});
